# P0-RAA-1 Audit Findings — `REDACTED_PG_PASSWORD` Leak

**Date:** 2026-06-11 · **Scope:** `/home/claw/invest-infra/` · **Mode:** read-only

---

## 1. Smoking Guns (3)

**SG-1. The "fix" commits regressed, not fixed.**
- `ffdb4f9` (2026-06-02) — added `__post_init__` validation in `config.py` ✅
- `5b0893b` (2026-06-04 21:13) — `chore: remove unused cron_alert_check.py` silently **re-introduced** `("PG_PASSWORD", "REDACTED_PG_PASSWORD")` as `setdefault` in `executor.py:38`, plus `woa_notify.py:44` and 6 `woa_tasks/task_*.py` files.
- `5fd2b75` (2026-06-09) — the "env-var fix": added 19 lines to `cron_dispatcher.py` only. **Did not touch** `executor.py`, `woa_notify.py`, `woa_tasks/*`, `cron_*.py` scripts, or `docker-compose.yml`. 22+ leaks still present.

**SG-2. WOA→CIA template embeds password at script-generation time.**
- `woa_notify.py:44` `PG_PASSWORD = "REDACTED_PG_PASSWORD"` → `:176` `.replace("REPLACE_PG_PWD", PG_PASSWORD)` → writes `woa_tasks/task_<tid>.py` with literal `password='REDACTED_PG_PASSWORD'` inlined.
- 8 generated task scripts on disk carry the literal (checked 2026-06-11). Anyone with `task_queue` consumer group `woa_workers` access can re-derive the password.
- **A second leak path**: `cron_morning_briefing.py:169,302,304` builds a prompt string containing the literal and writes it into `cia_task_queue` via `xadd` (Stream field `payload.prompt`). Confirmed in `evaluation_reports/morning_briefing_workflow_trace_report.md:62,187`.

**SG-3. Untracked `.bak` file in working tree + Gitee token in remote URL.**
- `data-pipeline/scripts/cron_pre_market.py.bak-20260611-015027` — contains the literal (line 7). Not in `.gitignore` (only `*.swp` covered). Any `git add .` commits it.
- `.git/config` remote: `https://chen-jian82:REDACTED_GITEE_TOKEN@gitee.com/...` — same `GITEE_TOKEN` in `.secrets/tokens.env:3`. Token baked into every clone URL.

---

## 2. 26 Leak Sites (grouped by pattern)

### A. Direct psycopg connection params (inline literal) — 6 sites
| File:Line | Context |
|---|---|
| `data-pipeline/src/factors/etf_fundamental.py:440` | `os.environ.update({"PGPASSWORD": "REDACTED_PG_PASSWORD"})` |
| `data-pipeline/src/factors/etf_info_flow.py:362` | same |
| `data-pipeline/src/factors/etf_risk.py:355` | same |
| `data-pipeline/scripts/woa_tasks/task_ae1dc426.py:15` | `password='REDACTED_PG_PASSWORD'` |
| `data-pipeline/scripts/woa_tasks/task_d1d2e0ca.py:15` | same |
| `data-pipeline/scripts/woa_tasks/task_4c7da74c.py:34` | same (generated by woa_notify.py) |

### B. URL/DSN format (postgres://) — 0 sites
No `postgresql://user:pass@` DSN leaks.

### C. Config dict / env-var fallback — 2 sites
| File:Line | Context |
|---|---|
| `data-pipeline/src/executor.py:38` | `("PG_PASSWORD", "REDACTED_PG_PASSWORD")` in `load_env()` setdefault — **regression source** |
| `data-pipeline/scripts/woa_notify.py:44` | `PG_PASSWORD = "REDACTED_PG_PASSWORD"` module-level constant |

### D. Redis payload (WOA→CIA) — 4 sites
| File:Line | Stream / key |
|---|---|
| `cron_morning_briefing.py:169` | in WOA prompt → `task_queue.payload.prompt` |
| `cron_morning_briefing.py:302,304` | in CIA prompt → `cia_task_queue.payload.prompt` |
| `woa_notify.py:81` (template → task_*.py:34) | `task_queue.payload` callback generates script with literal |

### E. YAML / docker-compose — 1 site
| File:Line | Context |
|---|---|
| `docker-compose.yml:32` | `POSTGRES_PASSWORD: REDACTED_PG_PASSWORD` (container init) |

### F. Hardcoded cron / shell scripts — 5 sites
| File:Line |
|---|
| `data-pipeline/scripts/cron_midday.py:7` |
| `data-pipeline/scripts/cron_post_market.py:7` |
| `data-pipeline/scripts/cron_market_collect_midday.py:26` |
| `data-pipeline/scripts/cron_market_data_collect.py:21` |
| `start.sh:54` (echo only — informational) |

### G. Module-level PG_PWD constants in woa_tasks — 6 sites
| File |
|---|
| `woa_tasks/batch_morning_briefing.py:13` |
| `woa_tasks/parallel_morning_briefing.py:15` |
| `woa_tasks/task_138125fe.py:23` |
| `woa_tasks/task_19066eae.py:23` |
| `woa_tasks/task_19523dea.py:23` |
| `woa_tasks/task_5b89d934.py:19` |
| `woa_tasks/task_eb540d7d.py:23` |

### H. Backup files — 1 site
| File |
|---|
| `data-pipeline/scripts/cron_pre_market.py.bak-20260611-015027:7` (untracked) |

### I. Documentation mentions (not source) — 1 file
`evaluation_reports/morning_briefing_workflow_trace_report.md:62,187`

**Subtotal:** 27 grep hits across 23 unique source files (+1 untracked `.bak`, 1 echo line, 2 report mentions).

---

## 3. PG Connection Surface

- **`psycopg2.connect()` call sites:** 63 Python files (incl. 21 `woa_tasks/task_*.py`).
- **Pattern split:** `pg.uri` / `pg.dsn` / `pg_cfg.uri` (config-driven, correct) — majority in `src/`; inline `host='localhost' port=5432 dbname='investdb' user='invest' password=…` — all woa_tasks + a few scripts.
- **Migration effort:** ~15 files with inline params need editing; the rest just need `.env` / `.secrets/` values to be correct.

---

## 4. Secrets Surface Area (full repo)

| Secret | Location | Status |
|---|---|---|
| `PG_PASSWORD=REDACTED_PG_PASSWORD` | `.env` (live), 23 source files, git history, Redis streams | 🔴 rotate + scrub |
| `MINIO_ROOT_PASSWORD=REDACTED_MINIO_PASSWORD` | `docker-compose.yml:92`, `executor.py:40` setdefault, `cron_*.py:5-6` env-set, `.env` | 🔴 same pattern |
| `MINIO_ROOT_USER=REDACTED_MINIO_USER` | `docker-compose.yml:91`, `executor.py:39` | 🟡 weak default |
| `CIFANG_TOKEN=ak_3_…` | `.env` (live), absent from `.secrets/` | 🟡 move to `.secrets/` |
| `MCP_TOKEN=lb_00…` | `.env` (live) | 🟡 move to `.secrets/` |
| `RSSCAST_TOKEN=Al9j7…` | `.secrets/tokens.env:2` | ✅ correct pattern |
| `GITEE_TOKEN=9487d5…` | `.secrets/tokens.env:3` + baked into remote URL | 🟡 rotate + clean remote |
| `WUDAO_API_KEY` | read at runtime from `.env` by `cls_news_coverage_test.py` | ✅ test-only |

**Reference model:** `.secrets/tokens.env` (gitignored dir, plaintext per-line). Currently holds only RSSCAST+GITEE; needs PG/MinIO/CIFANG/MCP added.

---

## 5. Git History Audit

- **First introduced:** `995d4fc` (2026-05-31 11:22) — "Phase 0: 基础设施"
- **Commits touching string:** 6 (`995d4fc`, `e88e376`, `ffdb4f9`, `26c0cf6`, `5b0893b`, `5fd2b75`)
- **Files ever carrying the literal:** 22 (incl. historical `data-pipeline/src/config.py`, `data-pipeline/progress.md`, `data-pipeline/scripts/cron_dispatcher.py`, `README.md`)
- **Branches:** `main` + `origin/main`; **6 tags** `v0.1.0`–`v0.6.0` reference pre-filter history
- **`filter-repo` blast radius:** rewrite 22 paths × 6 commits × force-push `main` + tag-deletion/recreate + Gitee fork notification. No submodules, no GitHub Actions, no PR history on remote.

---

## 6. Operational Constraints

- **PG user `invest`:** only created by docker-compose `POSTGRES_USER: invest` env. No `CREATE USER` in `init-db/*.sql`. Sole consumer of the password.
- **Running services depending on current password:**
  - `invest-postgres` (Up 6 days, healthy) — password baked into container env + `pgdata` volume
  - `invest-redis` (Up 6 days) — no auth (no `requirepass` in `redis.conf`)
  - `invest-minio` (Up 6 days) — `REDACTED_MINIO_USER/REDACTED_MINIO_PASSWORD` baked
  - Active PG connection from `172.19.0.1:49160` (Docker host) — openclaw/executor client
- **systemd timers:** user-level (`~/.config/systemd/user/cia_timer_*.service`) via `setup_cron_timers.sh`. No `cron.d` entries. Timers call `cron_dispatcher.py <task>` — no direct password ref.
- **No external PG consumers** detected beyond this host.
- **Rollback story:** none explicit. `pgdata` Docker volume = source of truth. Container recreation with old env = old password reinstated. No backup of pre-rotation state.

---

## 7. Remediation Order (7 steps)

1. **Rotate live password NOW** — `ALTER USER invest PASSWORD '<new>'` in PG; update `data-pipeline/.env`; recreate `invest-postgres` container (preserve `pgdata`). Same for MinIO root creds.
2. **Add PG creds to `.secrets/pg.env`** (gitignored); update `cron_dispatcher.py` env loader to read `.secrets/` after `.env`; add `CIFANG_TOKEN` + `MCP_TOKEN` to same pattern.
3. **Patch 4 code fallback sites:** `executor.py:38` (setdefault), `woa_notify.py:44` (constant), `cron_pre_market.py.bak-*` (delete), and the 4 cron scripts doing `os.environ['PG_PASSWORD'] = …` (`cron_midday`, `cron_post_market`, `cron_market_collect_midday`, `cron_market_data_collect`).
4. **Strip password from WOA/CIA prompts** — `cron_morning_briefing.py:169,302,304` replace literal with `<PG_CONNECTION>` placeholder; executor resolves at runtime from `.secrets/`.
5. **Refactor woa_notify template** — generated `task_*.py` should call shared `woa_lib.db` (reads `.secrets/`), not embed creds. Delete all 21 existing `woa_tasks/task_*.py` after refactor.
6. **`git filter-repo` for all 22 paths**; force-push `origin/main`; delete+recreate tags `v0.1.0`–`v0.6.0`; notify Gitee fork holders.
7. **Rotate `GITEE_TOKEN`**; reset remote URL without inline token (credential helper). Add `*.bak` + `*.orig` to root `.gitignore`. Add pre-commit `gitleaks` hook. **Re-Audit:** RAA checkpoints (grep=0, `os.environ.get("PG_PASSWORD")` everywhere, history clean, DB password actually rotated).

---

## 8. Key Risks (top 5)

1. **Live DB is exposed right now** — password in `.env` + container env + active client connections. Rotation is the only thing that actually closes the hole; code fixes are bookkeeping.
2. **Re-introduction risk** — three commits have already demonstrated the regression pattern. Without a pre-commit hook, next refactor will likely bring it back.
3. **Redis payload persistence** — `task_queue` / `cia_task_queue` may still hold historical XADD entries with the password in the `prompt` field. Stream trimming + key rename needed.
4. **Generated `woa_tasks/task_*.py` files** — 21 of them, not gitignored. On disk, executable, persist past session boundaries. Must be scrubbed + generation pattern fixed.
5. **Gitee mirror + tags** — `filter-repo` only rewrites this clone. External clones retain the original. Gitee `filter-repo` support + mirror sync are unknowns — verify before force-push.
