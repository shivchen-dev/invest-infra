[project]
name = "invest-domain"
version = "0.1.0"
description = "Pure domain models and ports for invest-infra-v2"
requires-python = ">=3.12,<3.15"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/invest_domain"]
