# 2026-06-15 Session

> 时长: 10:28→14:01+ GMT+8 · ~3.5h · 设计为主 + MCP debug

## Session Summary

Long session covering M4 (国海 11 模块落地到 ETF Dashboard) plan + Phase 0 execution.

### M4 Plan Evolution
- **v1**: 国海 11 模块直译 → 6 个"不该做"判定（❌ 错：没问终点就下结论）
- **v2**: 用户纠正「可视化是为网格交易准备」→ 网格改 P0
- **v3**: 用户再纠正「选股→交易→复盘闭环」→ 三段式架构
- **v3.5.3**: 最终版，含 broker 调研 + 汇报模块集成 + 省 9 天
- 6 个 plan 迭代（93b0023→10bf370→88b99b6→eec2ff7→dc295f0→3ea5ef3）

### Architecture Decisions
- 标的: ETF 专（不扩个股）
- 自动化: 半自动（GUI 调权重）
- 券商: 银河（非国海，之前截图只是参考对比）
- Broker 接入: 跳过/预留（星耀数智 cred 已存到 `.secrets/xingyao_credentials.json`）
- 闭环: 选股→交易→复盘→反馈到选股权重

### Broker Research
- 国海: 无公开程序化交易 API
- 星耀数智: TCP 120.86.124.106:8600 open, HTTPS 协议不兼容（可能是 FIX 或老 TLS）
- 银河: 有 QMT/miniQMT（xtquant Python 包）可申请，10万终身门槛
- Recommend: MiniQMT 路径，投研信号→同进程 xtquant 下单

### 汇报模块集成 Audit（CC audit, 1m49s）
- 7 Python 文件，2889 行，5 个安全集成点
- 省 9 天估时（25-31d → 16-22d）
- Key: `intraday_alert.IntradaySnapshotCache.get()` 同步只读，最适合 dashboard 轮询
- Key: `grid_analysis.compute_grid_signal()` + `grid_models.GridAnalyzer` 纯函数
- Key: post_market._build_risk_review / _extract_capital_flow 静态方法
- 5 个注意事项（async/conn/MCP/time/mutate），写进 findings.md

### Phase 0 ✅ Done (3 sub-steps in 1.5h)
1. **R0.1 摸底**: CC audit 7 文件，找集成接口
2. **PoC 1 intraday 路由**: Express 3 endpoints（types/latest/history），commit 2478fa2
3. **R0.2 数据管道 debug**（BLOCKER 修复）:
    - Root Cause: `mcpClient.ts` 调用错了服务器！
      - 配的是 `127.0.0.1:19100`（JiuwenClaw Gateway，chat agent）
      - 正确: `https://stock.quicktiny.cn/api/mcp-stream`
      - 方法名格式要求 jsonrpc 2.0 `tools/call（非裸 method）
      - 方法名应去掉 `wudao_aStock__` 前缀（bare `valuation_snapshot`)
      - 之前全部 32601 Method not found → catch 静默 → currentPrice=0
    - 更发现了 wudao_aStock MCP **vendor bug**: 不同 code 都返回相同数据（半导体的 26.23）
    - **实际修复**: 换源到 PG `etf_quotes`（data-pipeline `etf_kline` cron 每日 15:40 落库，441k 行）
    - 结果: 14 只 ETF 价格全正确，rebalance 13 个 alert 全工作
    - Commit: `3ea5ef3`

### Learnings (写入 .learnings/LEARNINGS.md)
1. 复杂项目方案设计第 1 问 = end-to-end 完整流程，不是"终点"
2. 汇报模块比预想成熟（2889 行），下次先 audit 再 plan
3. `mcpClient.ts` 踩坑：配了 JiuwenClaw Gateway 而不是 wudao_aStock MCP
4. CC session 污染：用 "RESET CONTEXT" + `<ctrl+c>` + 新 prompt 清旧任务
5. 重启 backend 必须先 `set -a; source .secrets/etf-backend.env; set +a`

### Open Items
- M3 残留 3 META（fix-status 里，待用户拍板）
- 星耀数智 spec 待拿（用户说跟银河沟通）
- Broker 真接入 = Phase X（等银河 broker 沟通结果）

### Project Paths
- Plan: `/home/claw/invest-infra/task-plans/2026-06-15_etf-11-modules-m4/`
- Secrets: `/home/claw/invest-infra/.secrets/xingyao_credentials.json`
- M3 fix-status: `/home/claw/invest-infra/RAA-EE/docs/CIA-ETF-2-fix-status.json`

## 15:14 — 投研系统 PG-First 铁律（持久化完成）

**用户原话：** "你记下投研系统数据以本地数据库为主，避免MCP等在线数据获取，没有数据就加强采集层，其他任何模块设计前提都是从PG获取"

**触发背景：** R0.2 数据管道修复（commit 3ea5ef3）— MCP vendor bug 导致 ETF 现价全错，改读 PG 解决

**5 步同步完成：**
- ✅ MEMORY.md — 新增 🚨 PG-First 铁律 区块（hot anchor）
- ✅ AGENTS.md — 新增 P0 #5 不可妥协 4 条
- ✅ TOOLS.md — 新增 MCP 教训章节（含 R0.2 复盘表）
- ✅ Memvid Smart Frame — title: "投研系统 PG-First 铁律"
- ✅ memory/2026-06-15.md — 本条

**应用 → Phase 1 选股 Dashboard：**
- 数据源：PG（`market_reports` / `etf_quotes` / `index_quotes` / `etf_alpha_signals` / `investment_memos`）
- 渲染：复用 cron 已跑的 `PreMarketFormatter` 输出，存到 `market_reports.messages`
- 禁止：Node 端查询时 fallback 到 MCP
