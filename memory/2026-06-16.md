# 2026-06-16 Session

> 时长: 06:36→07:55 GMT+8 · ~1h20m · Phase 1.5 v1.5-d 推进 + D.8 半做

## Session Summary

凌晨推进 v1.5-d 剩余 4 项 + 抓出 P0 bug (FK 反向) + 收尾半做 (LEARNINGS.md 4 条)。

## 时间线

| 时间 | 事件 | Commit |
|------|------|--------|
| 06:36 | 用户「Phase 1.5 v1.5-d 推进」+ 我列 3 选项 (A/B/C) | - |
| 06:39 | 用户选 C batch 1 (D.2 + D.5) | - |
| 06:42 | D.2 SQL 迁移 (candidate_pool +4 score columns) | `125dd87` |
| 06:48 | D.5 CandidatesPanel "加入持仓监控" 按钮 (CC 自报 88 行超 ≤40 指引, 1 prompt 自治) | `8a031a1` |
| 06:51 | 用户选 B 前半 (D.6 only) | - |
| 06:52-07:07 | D.6 cron-status API + watchdog — **CC 抓出 P0 bug**: FK `etf_candidate_pool_code_fkey` 反向阻塞 cron UPSERT | `25053d7` + `d518fc9` (FK fix) |
| 07:14 | 用户选 B 后半 1 (D.7 only) | - |
| 07:30 | D.7 CandidatesPanel cron 状态条 + R2.1 frontend 收尾 (CC 走 3 步绕路, CandidatesPanel.tsx D.7 改动曾丢失) | `dffadd4` + `03c8e89` |
| 07:51 | 用户选 D.8 半做 (只 .learnings) | - |
| 07:55 | LEARNINGS.md 4 条 P0 教训 + commit + push | `7dd2e91` (workspace) |

## v1.5-d 累计 8 commit 全部到位

| # | 改动 | Commit | 备注 |
|---|---|---|---|
| D.1.1 | filter_candidate_pool 真 UPSERT | `91d54ef` + `646ff5e` | RAA 修订阈值 |
| D.2 | candidate_pool +4 score columns | `125dd87` | 15 行 SQL |
| D.3 + D.4 | etf_portfolio.source + POST 自动 source | `2cb5295` | schema + 端点 |
| D.5 | CandidatesPanel "加入持仓监控" 按钮 | `8a031a1` | 88 行 (超指引 1 倍) |
| D.6 | cron-status API + watchdog | `25053d7` | 22 行 |
| **D.6.x** | **fix FK 反向** (P0 bug) | `d518fc9` | 1 DDL + R2.1 frontend 收尾 |
| D.7 | CandidatesPanel cron 状态条 | `dffadd4` | Tailwind flat, 不复用 antd |
| 额外 | R2.1 frontend 收尾 | `03c8e89` | 13 files, Phase 1 选股 Dashboard 遗物 |

## P0 bug 抓到 (D.6.x)

**FK 反向**:
```sql
etf_candidate_pool.code → etf_portfolio.code (ON DELETE RESTRICT)
```

**业务主轴**:
- 候选池 = cron 21:00 输出 top 20 (投研推荐)
- 持仓 = 用户决策固化
- 正确方向: 候选池独立于持仓 (D.4 source 字段已区分 candidate_pool/manual)

**修复**:
```sql
ALTER TABLE etf_candidate_pool DROP CONSTRAINT etf_candidate_pool_code_fkey;
```

**为什么 D.1.1 smoke test 没抓到**:
- Mock 5 行 (510300/510500/159618/159819/512690) **全在** 14 fixed portfolio 里
- 真 cron 输出 159033 等 14 fixed 之外 → FK 阻塞
- **P0 教训 #5**: smoke test 必须用真实 cron 数据 (D.6 commit 验证抓出)

## Lessons (写进 .learnings/LEARNINGS.md commit 7dd2e91)

1. **Smoke test 必须用真实 cron 数据** (D.1.1 + D.6.x FK bug 教训)
2. **业务主轴先于技术设计** (v1.5-a→c2→d 演进教训, "用户手动 join" 业务语义)
3. **CC commit 阶段 M 文件处理** (D.7 CandidatesPanel.tsx 改动丢失教训, 不要 git checkout HEAD -- file)
4. **v1.5-d 闭环业务价值复盘** (上游+中游+入口+监控就绪, 交易+复盘+反馈待 Phase 1.6+)

## 待做 (v1.5-d.1 后续)

- D.4 savePortfolio camelCase/snake_case type 不一致 (costPrice vs cost_price) — 后端兜底 OK, 但 type/wire 不一致
- D.6.x watchdog 改 PG 直读 (当前声明式, cron_dispatcher 没写 key, 运行时 watchdog 不真监控)
- D.8 收尾完整版 (progress.md 更新 + commit)

## 业务价值

**v3 决策 0「完整闭环」上游 + 中游 + 入口全部落地**:
```
候选池 (cron 21:00 真写 top 20, D.1.1)
  ↓ 用户手动 join (D.5 按钮 + 二次确认)
持仓 (D.4 source='candidate_pool' 自动标记)
  ↓ Review Dashboard 4 卡片 (R2.1 已有)
复盘 → 反馈 (Phase 1.6+ 待做)
```

**闭环完成度**: 60-70% (上游+中游+入口+监控就绪, 交易执行+复盘+反馈待 Phase 1.6+)

## 工具/过程

- claude-cmd-supervisor 在 D.6 阶段卡死 (idle 误判 busy), 用 `kill -9` 绕过, 之后直接 `tmux load-buffer + paste-buffer + send-keys Enter` 派单
- CC session 跨夜 9h (ccr-work 06-15 21:16 启动) 仍然健康, watch-dog 状态稳定
- 工作量: 5 个 prompt (D.2/D.5/D.6+FK fix/D.7+收尾/LEARNINGS) + 6 个授权 (1=Yes/2=allow all similar) + 1 个手写 LEARNINGS
