# Stage 4D Gate 1 真实链路验收记录

日期：2026-08-24
当前提交：`217cdc2` (`fix(pipeline): accept qualified symbols and idempotent retries`)

## 已验证事实

- Pipeline 全量测试：`2426 passed, 1 skipped`。
- WorkBuddy 聚焦测试：`31 passed`；Ruff 检查通过。
- PostgreSQL 中存在两条真实 2.0.0 候选运行：
  - `run_13e8593b12257afe`：`producer_status=succeeded`、`intake_status=accepted`、候选 `510300.SH`。
  - `run_dc1574b8250849a9`：`producer_status=succeeded`、`intake_status=accepted`、候选 `510500.SH`。
- 两条运行各有一条 `ExternalObservation`，当前 `admission_status=pending`。
- 历史真实候选 JSON `candidates_sector_2026-08-14_2026-08-14T145947-6t3b.json`（`schema_version=1.0`）通过当前适配器验证：提取 5 个候选、0 个拒绝，并完成临时归档演示。

## 2.0.0 异常路径证据矩阵

| 场景 | 当前行为 | 自动化证据 |
|---|---|---|
| 正常候选包 | 归档并写入 `ExternalWorkflowRun`、`ExternalArtifact`、`ExternalObservation` | 两条真实运行均为 `succeeded/accepted`；`test_gateway_imports_flat_candidates_files_and_ignores_legacy_audit_files` |
| 坏 JSON / 无法解析 | 移入 `candidate/failed/`，不写入准入记录 | `test_gateway_claims_invalid_flat_candidate_to_failed`、`test_gateway_other_exceptions_remain_under_failed_bucket` |
| 坏条目 | 保留有效候选，记录 `rejected_count` 与 findings | `test_bad_items_are_archived_with_findings` |
| 标的待解析 | 保留运行，输出 `needs_symbol_resolution` 诊断 | `test_gateway_exposes_diagnostics_on_success` |
| 相同内容重跑 | 归档层与导入层均幂等，不进入失败目录 | `test_gateway_idempotent_rerun_reports_both_layers_idempotent`、`test_gateway_idempotent_finish_when_same_flat_file_is_resubmitted` |
| 归档冲突 / 内容碰撞 | 冲突进入 `candidate/conflict/`；不同内容碰撞进入 `candidate/failed/` | `test_gateway_routes_archive_conflict_to_conflict_bucket`、`test_gateway_different_content_collision_routes_claimed_to_failed` |
| processing 恢复 | `--recover` 恢复可处理包；临时文件不被领取 | `test_gateway_ignores_tmp_files_and_recovers_processing_package`、`test_parser_accepts_recovery_mode` |

上述是 2.0.0 代码契约级证据；目前真实共享目录已验证正常链路，异常场景仍需在真实共享目录执行一次可回收的验收演练，才能将 Gate 1 从“代码已覆盖”推进为“真实链路已闭环”。

## 真实共享目录异常演练

### 自动化矩阵复测（2026-08-24）

- WorkBuddy 聚焦测试：`31 passed`；
- Pipeline 全量测试：`2426 passed, 1 skipped`；
- Ruff：`All checks passed`；
- `invest_pipeline.definitions` 导入检查通过。

本轮未修改业务代码，未产生数据库或共享目录测试残留。

### 坏批次（已完成）

- 输入：临时包 `candidates_arc_gate1_bad_batch_20260824.json`，内容为截断 JSON；
- 命令：`make workbuddy-result-import BRIDGE_ROOT=/home/claw/windows-ltsc/shared/workbuddy SOURCE_DIR=/home/claw/windows-ltsc/shared/workbuddy/candidate/results`；
- CLI 结果：`status=failed`、`observation_count=0`；
- 文件结果：输入从 `candidate/results/` 移入 `candidate/failed/`；
- 数据库核对：匹配该临时 workflow 标识的 `integration.external_workflow_runs` 记录为 `0`；
- 收尾：临时失败包已删除，`candidate/results/` 与 `candidate/failed/` 均无该测试包残留。

该演练证明坏批次不会进入 Observation 或 ExternalWorkflowRun。其余真实异常演练结果见下节。

### 剩余异常矩阵（已完成）

使用真实共享目录文件生命周期，适配器连接隔离内存 UoW，避免写入 PostgreSQL；演练输出如下：

| 场景 | 实际结果 |
|---|---|
| 坏条目 | `accepted=1`、`rejected=1`，同批有效条目继续归档 |
| 待解析标的 | `needs_symbol=1`，运行保持成功并输出诊断 |
| 重复导入 | 第二次 `archive_idempotent=true`、`import_idempotent=true` |
| 冲突包 | 首包成功，差异包进入 `candidate/conflict/` 并标记 `conflict=true` |
| processing 恢复 | `recover_once` 成功，恢复包进入 `candidate/archive/` |

所有演练包、归档目录和冲突文件已清理，测试命名空间无残留。

## 历史异常结论

`run_13e8593b12257afe` 的 `candidate/failed` 文件与已归档候选内容相同，但属于旧版重试行为：归档目标已存在时，旧版 `_finish()` 抛出 `FileExistsError`。当前提交已改为对相同内容执行幂等丢弃；该失败文件是历史残留，不代表数据库准入失败。

## 范围调整

2026-08-24 决定淘汰 legacy 1.1.1/1.1.2 三件套。该格式不再属于当前生产路径、Candidate Intake 或 Gate 1 验收依赖；相关历史合同保留为 retired 文档。

因此：2.0.0 正常候选准入链路、代码异常矩阵和真实共享目录异常演练均已完成，Gate 1 技术验收通过。下一步进入阶段 2 的只读 API/Web 工作台，不恢复或补做 legacy 兼容。

## Web/API 真实环境验收（2026-08-24）

- API systemd 服务：`active`；`GET /health` 返回 `200`；
- 只读接口：`GET /api/v1/research-center` 返回 `200`，真实响应为 `state=unavailable`，并正确展示 `empty/missing` 数据状态；
- CORS：来自 `http://127.0.0.1:3001` 的 `OPTIONS /api/v1/research-center` 返回 `200`，允许来源精确匹配，无 wildcard；
- Dashboard：真实 Web 页面加载成功，Research Center 未出现 `Failed to fetch`，无宿主路径、数据库连接串、凭据或 traceback 泄漏；
- 真实数据状态：Opportunity Radar 返回 2 条外部观察，准入状态保持 `pending`；Research、Candidate Pool、Market Freshness 的空/不可用状态均可解释；
- 页面唯一 `404` 为无已发布候选池时的 `candidate-pool/latest/diff` 空数据接口，页面保留可恢复的失败/空状态，不影响其他卡片。

本次联调发现并修复 systemd 默认 CORS 来源缺少正式 Web 端口的问题：默认允许 `localhost/127.0.0.1` 的 `3001` 与 `5173`，显式 `API_CORS_ORIGINS` 仍优先。修复后 API 全量测试 `588 passed`，Ruff 和 API import 检查通过。
