# Stage 4D Gate 2 端到端验收记录

日期：2026-08-25
代码提交：`4480cc9` (`feat: close gate2 external workflow observability`)

## 验收范围

验证 Fake WorkBuddy 候选包经过实际导入 CLI 后，能够进入 PostgreSQL，并由 API 与 Web 只读页面完整展示：运行、观测诊断和产物摘要。

## 新鲜 Fake 运行证据

- 临时 bridge root：`/tmp/invest-gate2-fake-aw7id9`；验收后已删除。
- 输入文件：`candidates_fake_gate2_20260825.json`。
- Fake workflow：`fake-gate2-20260825-1056`。
- CLI 结果：`status=success`、`observation_count=1`、`accepted_count=1`、`rejected_count=0`、`needs_symbol_resolution_count=0`。
- 归档结果：`archive_idempotent=false`、`import_idempotent=false`、`conflict=false`。
- 数据库运行 ID：`7ae46744-1775-5de0-93f3-5b841c9fcbf1`。

## API 验证

以下接口均返回 HTTP 200：

- `GET /api/v1/external-workflows/{run_id}`
- `GET /api/v1/external-workflows/{run_id}/observations`
- `GET /api/v1/external-workflows/{run_id}/artifacts`

关键响应事实：

- `producer=workbuddy`、`schema_version=2.0.0`、`producer_status=succeeded`、`intake_status=accepted`。
- 观测 `symbol=510500`、`admission_status=pending`、`candidate_status=pending_validation`。
- 观测原因为 `Gate 2 fake WorkBuddy acceptance candidate`。
- artifact URI 为 `archive://runs/2026-08-24/fake-gate2-20260825-1056/candidates.json`。

## Web 验证

使用真实 Chromium 浏览器访问 `http://127.0.0.1:3001/automation`，页面确认显示：

- Fake workflow ID `fake-gate2-20260825-1056`；
- 候选状态“候选待校验”；
- 原因摘要“Gate 2 fake WorkBuddy acceptance candidate”；
- artifact URI；
- 运行与观测计数。

同时访问 `/opportunity-radar`，确认候选雷达页面显示外部观测状态与原因。

## 部署后 smoke

- `GET http://127.0.0.1:8000/health`：`{"status":"ok","service":"invest-infra-v2"}`。
- API OpenAPI 包含 `candidate_status`。
- Web `http://127.0.0.1:3001/`：HTTP 200。
- 临时 bridge root、输入文件和归档文件均已清理；数据库保留验收运行记录用于追溯。

## 结论

Gate 2 的 Fake WorkBuddy 导入 → PostgreSQL → API → Web 只读展示链路已具备新鲜端到端证据。Gate 3 准入/证据闭环不在本次验收范围内。
