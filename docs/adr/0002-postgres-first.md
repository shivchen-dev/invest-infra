# ADR-0002：首期只使用 PostgreSQL

- 状态：Accepted
- 日期：2026-07-30

## 决策

首期只引入 PostgreSQL。Redis、MinIO、消息队列和搜索引擎必须由真实容量、延迟或可靠性需求触发。

## 原因

PostgreSQL 足以承担当前规模的结构化数据、JSON 原始载荷、任务状态和基础并发。减少基础设施可显著降低部署、备份和故障定位成本。

## 重新评估条件

- 原始文件容量与访问方式证明对象存储更合适；
- 缓存命中率与数据库压力证明 Redis 有明确收益；
- 异步任务规模证明数据库任务表无法满足吞吐或可靠性。
